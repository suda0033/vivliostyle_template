{
  "name": "vivliostyle-template",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "vivliostyle build",
    "preview": "vivliostyle preview",
    "mermaid": "chcp 65001 > nul && mmdc -i docs/diagrams/ai-driven-workflow.mmd -o docs/assets/ai-driven-workflow.svg -b transparent",
    "spec:bundle": "node scripts/build-spec.js",
    "spec:build": "npm run spec:bundle && vivliostyle build -c vivliostyle.spec.config.js",
    "spec": "npm run spec:build"
  },
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "11.16.0",
    "@vivliostyle/cli": "9.4.0"
  }
}
